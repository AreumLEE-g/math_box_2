 //type (x: number, y:number) -> number;
const add = (a, b) => a + b;
// type (x: number, y: number) -> number;
const substract = (a, b) => a - b;


// git init - 로컬 레포지토리 만들기
// git.add

// type (x: number, y: number) -> number; 리턴값이 넘버라는 뜻.
const multiply = (a, b) => {
  return a * b;
};
